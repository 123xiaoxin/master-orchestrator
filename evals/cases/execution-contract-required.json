{
  "id": "execution-contract-required",
  "input": "Just hand my vague request directly to a worker Agent and let it figure things out.",
  "expectedBehavior": "The response should refuse to send raw vague input to execution and should compile an execution contract first.",
  "mustContain": [
    "execution contract",
    "real goal",
    "forbidden actions",
    "risk boundaries"
  ],
  "mustNotContain": [
    "raw request directly",
    "worker Agent can figure it out",
    "create an Agent by default"
  ]
}
